hdl  ==> hdl() { 
		iverilog -o "$1" "$2" "$3" && vvp $1 && surfer dump.vcd 
		#iverilog -o sim.out rtl.v tb.v 
	      }

gup  ==> alias gup='xdg-open https://github.com/demonakaza03' #gup(github-user-profile)

cl   ==> alias cl = clear

gcln ==> alias gcln='git clone $1' 	#for git clone <repo_url>

iv   ==> alias iv='iverilog' #iverilog shortform

rtlf ==> alias rtlf='cd ~/Templates/professer_sergio/Practice/rtl/'
